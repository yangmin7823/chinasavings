"""
  SPA fallback: serve index.html for client-side routes.
  (Cloudflare Pages serves service-card.html automatically at /service-card.)

  /blog/ 与 /blog/<slug> 已在构建期被 prerender 成静态 HTML
  （dist/blog/index.html、dist/blog/<slug>/index.html），
  静态文件优先级高于本兜底规则，无需为 blog 写任何 redirect。
"""
/* /index.html 200
